# Python
__pycache__/
*.pyc
.venv/
venv/

# Raw fetched data is reproducible via the pipeline — keep repo light.
# BUT keep manual CSVs (delisted tickers like ROS that cannot be re-fetched).
data/raw/*
!data/raw/manual/
!data/raw/manual/**

# OS / editor
.DS_Store
Thumbs.db
.claude/

# Logs
*.log

# Note: data/processed/*.json and web/data/*.json ARE committed so GitHub Pages works.
